{
  "servers": {
    "plan-forge": {
      "args": [
        "pforge-mcp/server.mjs"
      ],
      "type": "stdio",
      "cwd": "${workspaceFolder}",
      "command": "node"
    },
    "openbrain": {
      "type": "sse",
      "url": "${env:OPENBRAIN_URL}",
      "headers": {
        "x-brain-key": "${env:OPENBRAIN_KEY}"
      }
    }
  }
}
