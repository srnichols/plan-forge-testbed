I verified all four entity and enum files against the slice spec before touching any code — they matched perfectly, so I left them untouched to avoid drift. The only modification was appending two fluent configuration blocks to `OnModelCreating` in `TimeTrackerDbContext.cs`, directly after the existing `TimeEntry` block, following the same pattern already established for Client, Project, and TimeEntry.

The key architectural decision was using `DeleteBehavior.Restrict` on both the `Invoice→Client` and `InvoiceLine→Project` foreign keys. This prevents the multiple-cascade-paths error that SQL Server would throw if both paths into `InvoiceLine` used `Cascade`, and it correctly preserves financial records when a client or project is removed. The `InvoiceLine→Invoice` relationship uses `Cascade` as required by spec.

For future slices: the `Client` navigation on `Invoice` uses `.WithMany()` (no inverse collection) rather than `.WithMany(c => c.Invoices)` — if a future slice adds an `Invoices` collection to the `Client` entity, this line in the DbContext will need updating. Similarly, `Project` has no `InvoiceLines` inverse collection currently. The decimal precisions follow a convention of `(18,2)` for money fields and `(5,2)` for percentage/hours fields — keep this consistent in any future entities dealing with billing.