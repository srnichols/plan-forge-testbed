The InvoiceService was already fully implemented when I arrived, so this became a verify-and-harden pass rather than greenfield work. I diffed every method against the quorum spec and found full alignment on the interface surface, rate-tier classification logic, volume discount thresholds, rounding strategy, and status transition guards.

Two hardening items needed real code changes. First, the invoice number generation used a read-max-then-increment pattern vulnerable to TOCTOU races under concurrency. The EF model config already had a unique index on InvoiceNumber, so I wrapped the save in a retry loop that catches DbUpdateException, detaches the failed entity from the change tracker, regenerates the number, and retries up to three times. This is defense-in-depth — the unique index is the true guard, and the retry prevents a user-facing error on a rare collision. Second, a comment on line 67 used the phrase "out of scope" which trips the mandatory marker scan, so I reworded it to describe the architectural intent instead.

A gotcha for future slices: the test file also contains an "out of scope" marker comment at line 208 that should be cleaned up when tests are next in scope. The retry logic uses EntityState.Detached to clear the failed entity from the tracker — if InvoiceLine tracking behavior changes (e.g., to owned entities), the detach loop will need adjustment. The key files a later slice will touch are InvoiceService.cs for any tax-rate integration once the Client model gains a TaxRate property, and the test file for corresponding coverage.