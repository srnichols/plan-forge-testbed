I arrived at this slice expecting to write the DashboardService implementation from scratch, but found it was already fully implemented from a prior session. The service file, interface, DI registration, and all five tests were already in place. Rather than making redundant changes, I verified the existing code against the plan's semantic rules — active-only filters on Clients and Projects, unfiltered counts on TimeEntries and Invoices, Draft+Issued predicate for outstanding totals, and locally computed NonBillableHours. Everything matched. The full test suite ran clean at 65 tests with zero failures, confirming no regression. Future slices touching the DashboardController should note that the service layer is complete and stable — no changes needed here.