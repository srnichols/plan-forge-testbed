I created ProjectsController as a direct structural mirror of ClientsController, which was the authoritative template for this codebase. The key verification step was confirming that IProjectService.UpdateAsync returns Task<Project> (not Task), which meant the PUT action could return Ok(updated) rather than NoContent. The GetAll endpoint adds an optional clientId query parameter that the ClientsController doesn't have, since the project service supports filtering by client. All exception handling follows the established pattern: ValidationException maps to 400 with a simple error object, KeyNotFoundException maps to 404, and infrastructure errors bubble up naturally. No modifications were needed outside the single new file — Program.cs already had the service registered and the routing conventions automatically expose the endpoints at /api/projects. Future slices touching this controller should note that there's no integration test coverage yet, and the exception-driven control flow is a known tech debt item that could be addressed with a global ProblemDetails middleware in a later phase.