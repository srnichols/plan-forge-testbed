I followed the established ClientServiceTests pattern exactly — same IDisposable structure, in-memory database with unique name per instance, and direct DbContext access for verifying state that the service API intentionally hides (like deactivated projects). The SeedClientAsync helper returns the full Client entity rather than just the ID, which proved useful for asserting ClientId on results. I used unique project names across tests to avoid tripping the duplicate-name guard in CreateAsync. For the non-existent client test I used ID 99999 rather than seeding and deactivating a client, keeping the failure cause unambiguous. Exception assertions are type-only — no message string matching — which will survive wording changes in ProjectService. The GetAllAsync filter test seeds two clients with one project each, then verifies both the filtered and unfiltered paths in a single Fact. Future slices touching ProjectService tests should be aware of the duplicate-name constraint and the IsActive filter on GetByIdAsync/GetAllAsync.