This slice was verification-first as the quorum analysis predicted. The test file already existed with all five required methods matching the contract precisely. The harness correctly uses a GUID-unique InMemory database per test instance, the seed helper inserts a client for FK satisfaction, and the deactivation test wisely re-fetches via FindAsync rather than going through the service layer. No edits were needed — the file passed the contract and the validation gate on first run. Future slices touching ProjectService should note that the tests rely on ValidationException being thrown for empty names and non-existent clients, so any refactoring of the service's validation logic must preserve those exception types. The test project already has all needed NuGet references in place.