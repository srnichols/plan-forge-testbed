The slice was already functionally complete when I started, so the work was purely hardening refinements — four surgical edits in a single file. I applied them all in one pass to minimize diff churn: null-guards on request DTOs, a shared validation helper extracted to match the pattern already established by ClientService, string interpolation consistency, and AsNoTracking on every read path including the client-existence check in CreateAsync where I also narrowed the projection to just Id and IsActive.

The client-existence query change from loading the full Client entity to a projected anonymous type with AsNoTracking was the most impactful refinement. It avoids pulling the client into the change tracker, which would have been confusing since we only need to verify existence and active status — we never mutate the client here. Future slices touching CreateAsync should be aware that `client` is now an anonymous type, not a `Client` entity.

I kept case-sensitive duplicate-name matching as-is. Both quorum models flagged this as ambiguous in the spec, and changing it would risk breaking existing tests or data expectations. This should be raised as a product question before any future slice tackles it.

The Description null-coalescing was simplified from the ternary `request.Description == null ? null : request.Description.Trim()` to `request.Description?.Trim()` — functionally identical but cleaner. Future slices adding fields to the request DTOs should follow this pattern.

Key files for later slices: ProjectService.cs (this file), ProjectServiceTests.cs (existing tests that validated these changes remain green), and ClientService.cs (the pattern reference for validation helpers).