The existing draft service materialized all matching time entries to memory and performed grouping and aggregation client-side. This is a classic N+1-adjacent anti-pattern for reporting queries — it pulls far more data over the wire than needed and does work the database is better at.

I refactored all three methods to push GroupBy and Sum/Count aggregations into the EF Core expression tree so they translate to SQL GROUP BY. The key risk was DateOnly-to-DateTime conversion inside expression trees — different EF providers handle DateOnly.FromDateTime differently (Npgsql supports it, SQLite and InMemory may not). I eliminated this by hoisting the conversion into a private static helper that computes DateTime bounds before the query is built. This also naturally gave us the exclusive upper-bound pattern (end.AddDays(1)) which avoids tick-precision edge cases with inclusive end dates.

For GetProjectBreakdownAsync, I replaced the Include-based eager load with a server-side GroupBy on ProjectId and Project.Name, which avoids materializing the full Project entity. The percentage calculation and ordering remain in-memory since they depend on the total, but the input is now just the aggregated rows — bounded by project count, not entry count.

I introduced a private sealed record ProjectRow to hold the intermediate server-side projection, keeping the anonymous type usage local to the EF query and giving the in-memory projection a clean typed input. The DailyTimeline method groups by e.Date.Date server-side, which EF Core translates to a date truncation, then projects to DateOnly.FromDateTime in memory after materialization.

A future slice touching this file should watch for: the e.Project!.Name null-forgiving operator assumes ProjectId is a required FK with a valid navigation — if tests reveal null Projects, switch to an explicit join. Also, the GroupBy(_ => 1) trick for single-row aggregation works on Npgsql and SQL Server but should be verified if the provider changes. The response DTOs have a specific parameter order (TotalHours first, PeriodStart/PeriodEnd last for HoursSummaryResponse) that differs from what you might expect — always check the record definition.