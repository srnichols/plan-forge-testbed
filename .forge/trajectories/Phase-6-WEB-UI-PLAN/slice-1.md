I found that all Slice 1 artifacts were already in place when I started. The Web.Client project was fully scaffolded with the correct SDK, target framework, and project reference to Core. All eight DTO files existed under Models, the five API interface/implementation pairs were present at the project root, and ServiceCollectionExtensions registered all five typed HttpClient pairs via AddTimeTrackerClient. The solution file already had the project listed under the /src/ folder.

The key pattern established here is that API client interfaces live at the project root (not in a subfolder), implementations sit alongside them, and DTOs live under Models with a clear split between read DTOs and form models with DataAnnotations. The ServiceCollectionExtensions uses AddHttpClient with typed clients, setting BaseAddress from the provided base URL. Future slices consuming these clients should inject the interfaces (IClientsApi, IProjectsApi, etc.) and expect HttpClient-based implementations that serialize/deserialize JSON against the REST API.

No dead-ends or gotchas encountered since the work was already complete. A future slice wiring up the Blazor UI should reference AddTimeTrackerClient in Program.cs and ensure the base URL points to the running API.