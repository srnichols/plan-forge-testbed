The existing partial scaffold was already well-aligned with the slice contract — all 20 files existed with correct naming, correct API routes, correct DTO shapes, and proper primary-constructor patterns. The InvoiceDto file already contained GenerateInvoiceFormModel and VoidInvoiceFormModel with correct shapes matching the InvoicesController. The only gaps were the csproj missing LangVersion and TreatWarningsAsErrors, and ServiceCollectionExtensions lacking input validation and trailing-slash normalization for the base URL. Both were straightforward edits. The full solution built clean on first try with warnings-as-errors enabled, meaning the existing nullable annotations were already correct. No JsonStringEnumConverter was found in the API's Program.cs, confirming enums serialize as ints — future slices consuming enum values should be aware. The IClientsApi returns ClientDto from UpdateAsync rather than void as the quorum spec suggested, but this matches the actual controller behavior and is more useful, so I kept it. Key files for later slices: ServiceCollectionExtensions.AddTimeTrackerClient is the DI entry point; all API paths use relative URIs like api/clients, api/projects, api/timeentries, api/invoices, api/dashboard. The InvoicesApi uses POST to sub-paths for state transitions (issue, pay, void).