I approached this as an idempotent refresh — most files already existed from a prior slice, so I read each one, compared to the spec, and applied surgical edits only where content drifted. The biggest gotcha was the FluentUI Icons namespace: the `Icons` static class lives in `Microsoft.FluentUI.AspNetCore.Components.Icons` and requires a using alias (`@using Icons = Microsoft.FluentUI.AspNetCore.Components.Icons`) in `_Imports.razor`. Without this alias, `Icons.Regular.Size20.*` references fail with CS0246. This is not documented in most quick-start guides but is clearly stated on the NuGet package README.

The Routes.razor also needed care — the original used `typeof(App).Assembly` and `typeof(Components.Layout.MainLayout)` without `@` prefixes, which compiled fine but drifted from the spec's `@typeof(Program).Assembly` pattern. Using `Layout.MainLayout` without the `Components` prefix failed because `_Imports.razor` imports `TimeTracker.Web.Components.Layout` not just `Layout` as a bare namespace — the Razor compiler can't resolve the short form.

Future slices touching NavMenu should know that icons use the `new Icons.Regular.Size20.IconName()` pattern with the alias. The `FluentBodyContent` component is correct for the main content area — `FluentMain` also works but doesn't match the spec and may have different styling behavior. The `FluentToastProvider` and `FluentDialogProvider` were kept in MainLayout even though the spec didn't mention them — they're needed by downstream pages that use toast notifications and dialogs. Key files for future slices: `_Imports.razor` for global usings, `MainLayout.razor` for layout changes, and `NavMenu.razor` for adding new navigation items.