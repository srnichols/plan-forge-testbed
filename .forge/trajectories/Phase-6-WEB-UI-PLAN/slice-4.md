This slice was an alignment pass, not a greenfield build. All four Clients files already existed and were structurally sound — the work was comparing them against the canonical Projects pages and fixing cosmetic drift. The two meaningful deviations were the verbose rendermode directive (which Projects had already normalized to the short-form InteractiveServer) and the Cancel button using Outline instead of Neutral appearance. The Min attribute on FluentNumberField using a string "0.01" instead of the spec's suggested decimal literal was left as-is since the existing string form works correctly with FluentNumberField and matches how the underlying HTML min attribute functions. Future slices touching these pages should know that IClientsApi, ClientDto, and ClientFormModel all live in TimeTracker.Web.Client and must not be modified from within the Pages scope. The Dispose pattern consistently cancels the CancellationTokenSource but does not dispose it — this matches the Projects pattern and is intentional since the CTS is readonly and short-lived with the component.