I arrived at this slice to find all ten files already fully implemented. The Projects pages follow the exact Clients shape — list with FluentDataGrid, edit with dual-route EditForm, string-proxy bridge for the ClientId FluentSelect, CancellationTokenSource lifecycle, and two-tier exception handling. The TimeEntries pages add a project filter select at the top of the list and proxy fields for DateTime and decimal-to-double conversion on the create form. The Invoices list requires a client selection before showing data, since the API only exposes GetByClientAsync, and uses a FluentBadge with a switch on InvoiceStatus for visual differentiation. IsActive is displayed read-only in the Projects grid but omitted from the edit form, since ProjectFormModel does not expose it and modifying models is out of scope. The InvoicesList code-behind imports TimeTracker.Core.Models for the InvoiceStatus enum, which is safe per the layer-purity regex gate. Future slices touching these pages should be aware that the rendermode shorthand InteractiveServer is used rather than the explicit constructor form seen in Clients — both compile identically.