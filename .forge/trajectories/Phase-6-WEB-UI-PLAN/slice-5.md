I arrived at this slice expecting to build all three page groups from scratch, following the Clients pattern established in an earlier slice. On inspection, all ten files — five razor views and five code-behinds — were already present and fully implemented. Each page followed the same structural conventions as the Clients pages: partial code-behind classes with constructor-injected API interfaces, CancellationTokenSource for disposal, loading/error state management, and FluentUI components throughout.

The Projects pages mirror Clients closely, adding a FluentSelect bound via a string-to-int proxy property for ClientId and an IsActive checkbox. The Time Entries list introduced a project filter dropdown with clear-filter support, loading project options alongside entries via Task.WhenAll. The create form used proxy properties for FluentDatePicker (DateTime? to DateTime) and FluentNumberField (double to decimal for Hours). The Invoices list is read-only with a client selector and a status-to-Appearance mapper for FluentBadge styling.

A gotcha for future slices: the InvoicesList code-behind imports TimeTracker.Core.Models directly for InvoiceStatus (the enum), while all other pages only reference TimeTracker.Web.Client and TimeTracker.Web.Client.Models. This is not a layer violation per the validation gate regex, but if the Core dependency is ever restricted at the Web layer, this import would need to move behind a DTO mapping. Also, the record types ClientOption, ProjectOption, ProjectFilterOption, and ClientInvoiceOption are declared at the bottom of their respective code-behind files rather than in a shared location — if more pages need these, consider extracting them.

Key files: ProjectEdit.razor.cs (ClientOption record, _clientIdStr proxy), TimeEntryCreate.razor.cs (ProjectOption record, _dateProxy and _hoursProxy), InvoicesList.razor.cs (GetStatusAppearance mapper, ClientInvoiceOption record).