I approached this slice as a verify-and-gap-close pass since the quorum synthesis indicated the code already existed. After reading both the service implementation and its test file, I confirmed the implementation perfectly matches the specification: file-scoped namespace, interface with all five methods accepting CancellationToken, record DTOs, partial class with GeneratedRegex, proper validation throwing ValidationException with exact message strings, null email coercion to string.Empty, whitespace trimming, and KeyNotFoundException for missing/inactive entities on update and deactivate.

The validation gate passed cleanly on first run — zero edits required. The pattern established here (interface + DTOs + implementation in a single file, primary constructor with DbContext, private static validation method) mirrors ProjectService.cs and should be followed by any future service slices. Future slices touching controllers should note that the service throws ValidationException and KeyNotFoundException — the controller layer will need to map these to appropriate HTTP status codes (400 and 404 respectively).