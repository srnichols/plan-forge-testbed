I chose to follow the unified plan precisely — creating a reusable ApiWebApplicationFactory and a single lifecycle integration test. The main gotcha was the dual-provider conflict: simply removing DbContextOptions descriptors is insufficient in .NET 10 with EF Core because Npgsql registers internal services beyond just the options. The fix was to remove all service descriptors whose ServiceType contains "EntityFrameworkCore" in its full name, which cleanly strips the Npgsql provider before re-registering with InMemory. A second minor issue was the Location header case sensitivity — ASP.NET Core's `[Route("api/[controller]")]` uses PascalCase controller names, so the Location comes back as `/api/Clients/1` not `/api/clients/1`. The assertion needed OrdinalIgnoreCase. Future slices adding controllers should reuse ApiWebApplicationFactory directly and be aware of the PascalCase routing in Location headers. Key files for future work: `tests/TimeTracker.Tests/Infrastructure/ApiWebApplicationFactory.cs` for the factory, and the pattern in `ClientsApiIntegrationTests.cs` as a template for other entity lifecycle tests.