# Agents & Automation — Template

> **Purpose**: Document background workers, event processors, scheduled tasks, and agent patterns for your project.  
> **Instructions**: Fill in each section with your project's specific agents and automation.

---

## Agent Architecture

<!-- Describe your service communication patterns: pub/sub, queues, cron, etc. -->

### Communication Patterns

| Pattern | Technology | Use Case |
|---------|-----------|----------|
| Pub/Sub | (Redis / NATS / Kafka / RabbitMQ) | Event-driven processing |
| Request/Response | (HTTP / gRPC) | Synchronous service calls |
| Scheduled | (Cron / Timer / Celery Beat) | Periodic tasks |

---

## Background Services

### Worker 1: <Name>

**Location**: `path/to/worker`  
**Purpose**: (What it does)  
**Technology**: (BackgroundService / Celery / Bull / etc.)

**Key Features**:
- (feature 1)
- (feature 2)

**Configuration**:
```
# Add your worker's config here
```

---

## Event Processing

### Topic/Queue: <name>

**Publisher**: (which service publishes)  
**Subscriber**: (which service consumes)  
**Schema**: (event structure)

---

## Monitoring

### Health Checks
- (list health check endpoints)

### Metrics
- (list key metrics to monitor)

---

## Pipeline Agents

The following agents automate the full Specify → Plan → Execute → Review → Ship workflow with handoff buttons:

| Agent | File | Purpose | Tools |
|-------|------|---------|-------|
| Specifier | `.github/agents/specifier.agent.md` | Interviews user to define what & why (Step 0) | read, search |
| Plan Hardener | `.github/agents/plan-hardener.agent.md` | Hardens draft plans into execution contracts | read, search, editFiles |
| Executor | `.github/agents/executor.agent.md` | Executes slices with validation gates | read, search, editFiles, runCommands |
| Reviewer Gate | `.github/agents/reviewer-gate.agent.md` | Read-only audit for drift and violations | read, search |
| Shipper | `.github/agents/shipper.agent.md` | Commits, updates roadmap, captures postmortem, push/PR | read, search, editFiles, runCommands |

**Handoff chain**: Specifier → *"Start Plan Hardening →"* → Plan Hardener → *"Start Execution →"* → Executor → *"Run Review Gate →"* → Reviewer Gate → *"Ship It →"* (PASS) or *"Fix Issues →"* (LOCKOUT) → Shipper

> These are functionally identical to the copy-paste prompts in the [Runbook Instructions](docs/plans/AI-Plan-Hardening-Runbook-Instructions.md). Use `/create-agent` to add project-specific agents.

---

## Development & Debugging

```bash
# How to run workers locally
<YOUR WORKER START COMMAND>

# How to view worker logs
<YOUR LOG COMMAND>
```
